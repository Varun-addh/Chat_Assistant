# Copy this file to .env and fill values before running docker-compose or docker run
# This file lists the environment variables the app expects. DO NOT put real secrets
# into a file you plan to commit. Keep the real `.env` out of source control.

# ----------------------------- Provider / LLM keys ---------------------------
LLM_PROVIDER=gemini
GEMINI_API_KEY=
GEMINI_MODEL=
GROQ_API_KEY=
GROQ_MODEL=
GROQ_MAX_TOKENS=
OPENAI_API_KEY=
COHERE_API_KEY=

# ----------------------------- App / Runtime keys ---------------------------
API_KEY=
SERPER_API_KEY=
JUDGE0_API_KEY=
GITHUB_TOKEN=

# Feature flags (true/false)
ENABLE_HYBRID_SEARCH=true
ENABLE_RERANKING=true
ENABLE_CODE_EXECUTION=true
ENABLE_QUERY_EXPANSION=true
ENABLE_STREAMING=true

# App settings
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000

# Optional: override vector DB / storage paths when mounting volumes
DATA_DIR=/app/data

# Notes:
# - Fill the values in your local `.env` (not here). Keep secrets out of Git.
# - For production, store secrets in a secret manager or use Docker/K8s secrets.
